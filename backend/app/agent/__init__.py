"""The agent API's read models: parts, documents, comparison, rendering.

A report is written by an LLM from finished runs. Everything it needs is
assembled here from rows the platform already keeps — nothing is re-measured
and nothing is re-derived that a stored row already says.
"""
